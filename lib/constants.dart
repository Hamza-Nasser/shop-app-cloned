import 'package:flutter/material.dart';

const defaultPadding = 15.0;
const smallScreenHeight = 500;
const midScreenHeight = 700; // large screens height is higher than 700.

// fontFamily

const grandisExtendedFont = "Grandis Extended";

// duration

const Duration defaultDuration = Duration(milliseconds: 300);

//colors

const Color defaultColor = Color.fromARGB(255, 47, 149, 149);
const Color accentColor = Color.fromARGB(255, 5, 102, 114);
